name: PR CI

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  shared-ci:
    uses: ./.github/workflows/shared-ci.yml
    with:
      fetch-depth: 1
      run-audit-signatures: false
